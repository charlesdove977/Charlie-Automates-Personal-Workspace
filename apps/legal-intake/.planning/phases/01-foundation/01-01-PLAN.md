---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Initialize the Next.js project with TypeScript, Tailwind CSS, and development tooling.

Purpose: Establish the foundational codebase structure that all subsequent phases will build upon.
Output: Working Next.js 14 app with TypeScript, Tailwind CSS, App Router, and development environment configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech decisions:**
- Next.js 14 with App Router (modern React patterns, server components)
- TypeScript (type safety for legal domain)
- Tailwind CSS (rapid UI development, professional appearance)
- pnpm (faster, disk-efficient package manager)

**UX Philosophy from PROJECT.md:**
Lawyers must feel: "This won't get me sued."
- Boring, safe, professional UI
- No experimental UX
- Clear compliance disclaimers throughout
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 project</name>
  <files>package.json, tsconfig.json, tailwind.config.ts, postcss.config.mjs, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
    Create Next.js 14 project in the legal-intake directory:

    1. Run: `pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm`
    2. Accept defaults for remaining prompts
    3. Update `src/app/page.tsx` to show a simple "Legal Intake System" heading (professional, minimal)
    4. Update `src/app/layout.tsx` metadata:
       - title: "Legal Intake | Case Triage System"
       - description: "AI-powered case intake and triage for law firms"
    5. Clean up `src/app/globals.css` to remove demo styles, keep Tailwind directives only

    DO NOT add any fancy animations or experimental UI - keep it professional and boring per UX philosophy.
  </action>
  <verify>
    cd apps/legal-intake && pnpm dev
    Visit http://localhost:3000 - should show "Legal Intake System" heading
    pnpm build - should complete without errors
  </verify>
  <done>Next.js app runs locally, build succeeds, TypeScript compiles, Tailwind works</done>
</task>

<task type="auto">
  <name>Task 2: Set up development environment</name>
  <files>.env.example, .env.local, .gitignore, README.md</files>
  <action>
    Configure development environment:

    1. Create `.env.example` with placeholder variables:
       ```
       # Database
       DATABASE_URL="postgresql://..."
       DIRECT_URL="postgresql://..."

       # Supabase
       NEXT_PUBLIC_SUPABASE_URL="https://..."
       NEXT_PUBLIC_SUPABASE_ANON_KEY="..."
       SUPABASE_SERVICE_ROLE_KEY="..."

       # AI (Claude)
       ANTHROPIC_API_KEY="sk-ant-..."
       ```

    2. Create `.env.local` as a copy (will be gitignored, user fills in real values)

    3. Update `.gitignore` to include:
       - .env.local
       - .env*.local
       - node_modules
       - .next
       - out

    4. Create minimal `README.md`:
       - Project name and one-line description
       - Setup instructions (pnpm install, copy env, pnpm dev)
       - No lengthy docs yet - just enough to get running

    DO NOT add complex tooling like Husky, lint-staged, or CI/CD config yet - keep it simple.
  </action>
  <verify>
    cat .env.example - should show all required variables
    cat .gitignore - should include .env.local
    cat README.md - should have setup instructions
  </verify>
  <done>Environment files exist, gitignore configured, README has setup instructions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` succeeds
- [ ] `pnpm dev` starts dev server on port 3000
- [ ] `pnpm build` completes without errors
- [ ] TypeScript compiles without errors
- [ ] Homepage displays "Legal Intake System"
- [ ] .env.example exists with all required variables
- [ ] README.md has setup instructions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- No ESLint errors
- Project structure follows Next.js 14 App Router conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
